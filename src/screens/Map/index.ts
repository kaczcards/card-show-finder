import MapScreen from './MapScreen';

// Re-export the correctly-named component (the leading underscore was added by mistake)
export { MapScreen };

// Default export for convenience when importing the main screen
export default MapScreen;
