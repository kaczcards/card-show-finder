# Learn more https://docs.github.com/en/get-started/getting-started-with-git/ignoring-files

# dependencies
node_modules/

# Expo
.expo/
dist/
web-build/
expo-env.d.ts

# Temporary build artifacts (may contain secrets)
temp-export/
temp-test-build/

# Native
.kotlin/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Metro
.metro-health-check*

# debug
npm-debug.*
yarn-debug.*
yarn-error.*

# macOS
.DS_Store
*.pem

# local env files
.env*.local
# primary env file (contains Supabase keys, etc.)
.env

# mobile service credentials (never commit)
google-services.json
GoogleService-Info.plist

# typescript
*.tsbuildinfo

# ─────────────────────────────────────────────────────────────
# E2E / Detox
#   These directories are generated automatically when running
#   Detox-based end-to-end tests.  They may contain large logs,
#   screenshots, videos and HTML reports that should **not** be
#   committed to version control.  Keep the `.gitkeep` files so
#   the folders themselves remain in the repo.
# ─────────────────────────────────────────────────────────────
e2e/artifacts/
e2e/reports/

# Keep placeholder files
!e2e/artifacts/.gitkeep
!e2e/reports/.gitkeep
